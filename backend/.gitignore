# Secrets & local env
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build output is generated/published by CI; ignore local runs
public/dashboard.json

# NOTE: config.json is intentionally NOT ignored - it contains no secrets
# (only names, weekly goals, source types, and the NAMES of env vars) and
# CI needs it. Real credentials live in .env / GitHub Actions secrets.
